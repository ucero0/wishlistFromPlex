"""Plex module route constants."""

# Base module prefix - change this to rename the entire module
MODULE_PREFIX = "/plex"

# Sub-route prefixes (derived from MODULE_PREFIX)
USERS_PREFIX = f"{MODULE_PREFIX}/users"
WISHLIST_PREFIX = f"{MODULE_PREFIX}/wishlist"
SYNC_PREFIX = f"{MODULE_PREFIX}/sync"

