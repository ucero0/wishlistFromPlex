# Shell scripts run inside Linux containers — always LF (never CRLF on checkout)
*.sh text eol=lf
infra/** text eol=lf

# Prowlarr SQLite DB — treat as binary
infra/prowlarr/config/prowlarr.db binary
