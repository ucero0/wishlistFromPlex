#!/usr/bin/with-contenv bash
# Deluge daemon creates /config/auth only after it starts — run credential setup here, not in cont-init.
set -euo pipefail

until /scripts/configure-deluge-credentials.sh; do
  echo "[deluge-init] Waiting for Deluge daemon before applying credentials..." >&2
  sleep 5
done

exec sleep infinity
